M2m project built with the PHP framework called Laravel and using Docker.

IMPORTANT:
This project ONLY runs on localhost:8000. To start it, you need Docker installed and must run the following command:
- docker-compose up
